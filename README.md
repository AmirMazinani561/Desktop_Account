# نرم‌افزار حسابداری تحت وب — مخزن اسناد و نقشه راه

این مخزن در حال حاضر **فقط اسناد** پروژه است (هنوز کدی نوشته نشده؛ طبق توافق، تا تأیید کارفرما کدنویسی شروع نمی‌شود).

## اسناد به ترتیب خواندن

| فایل | چیست | نسخه |
|---|---|---|
| [`Agents.md`](Agents.md) | قوانین کار ایجنت‌ها روی این ریپو (زبان، گیت، تست، محیط) | ۲ |
| [`PRD.md`](PRD.md) | سند نیازمندی‌ها و نقشه راه محصول در ۵ فاز (۰ زیرساخت ← ۱ شخصی/MVP ← ۲ کسب‌وکار ← ۳ شرکتی ← ۴+ تولید و آینده) | ۰.۵ تأییدشده |
| [`DATA_MODEL.md`](DATA_MODEL.md) | طراحی مدل داده: موجودیت‌ها، قیدها، تریگرهای تضمین تراز، شماره‌گذاری و کدگذاری خودکار، سناریوهای کلیدی | ۰.۴ تأییدشده |
| [`Agent_worklog.md`](Agent_worklog.md) | حافظهٔ پروژه: تصمیم‌های کارفرما، لاگ پیشرفت، موارد باز، بک‌لاگ | — |

## ایدهٔ اصلی در یک پاراگراف

یک **موتور مالی سندمحور (دوبل‌انتری)** که از روز اول ساخته می‌شود: هر تراکنش سادهٔ شخصی در واقع یک **سند دوآرتیکلی خودکار** است و فاکتور، چک، سند دستی و تراکنش‌های دریافتی از منابع خارجی (پیامک بانکی، لینک «کیف پول هوشمند») همگی روی همان موتور می‌نشینند.
نتیجه: **یک مدل داده، سه سطح تجربهٔ کاربری** (شخصی ← کسب‌وکار کوچک ← شرکتی) به‌جای سه سیستم جدا.

## وضعیت فعلی

✅ هر دو سند (PRD ۰.۴ و مدل داده ۰.۳) **تأیید شده‌اند** (۱۴۰۵/۰۶/۲۳) و ۵ پاسخ قطعی نوبت دوم هم ثبت شد (انتقال فقط افزایشی کیف پول، سال مالی قابل بستن/گشایش، ویرایش/حذف مجاز، بدون ثبت خودکار پیامک، بدون چندزبانه/چندارزی).
**فاز ۰ در جریان است:** `db/prisma/schema.prisma` (۲۰ مدل/۲۹ enum) + `db/sql/0001_*.sql` (تریگرها/قیدها/viewها) ساخته و با پارسر واقعی Postgres اعتبارسنجی شد.

## قدم بعدی

۱) `db/seed/system_accounts.sql` (حساب‌های سیستمی) ۲) `lib/jalali.ts` + تست کدپوینت ۳) `db/sql/0002_phase1_*.sql` (دوره‌ای/بودجه/وام/پیامک) ۴) تست‌های طلایی ۱۰.۱–۱۰.۴ و تست‌های رد ۵) دریافت `src/db/schema.ts` کیف پول برای قطعی‌کردن بند ۱۱ مهاجرت.
