# لاگ کاری ایجنت — حافظه پروژه (AGENT WORKLOG)

> **هدف این فایل:** حافظه مکتوب ایجنت در ریشه پروژه. هر تصمیم مهم، هر قدم انجام‌شده و هر قانون دائمی اینجا ثبت می‌شود تا در طول پروژه (حتی اگر طولانی شود) چیزی گم نشود.
>
> **قانون به‌روزرسانی:** ایجنت موظف است این فایل را در این مواقع به‌روز کند:
> ۱) پایان هر قدم/مایلستون ۲) هر تصمیم جدید کارفرما ۳) هر تغییر مسیر فنی ۴) شروع و پایان هر فاز اجرایی.
> زبان مستندات: فارسی.

---

## ۱. زمینه پروژه (خلاصه)

- مبنا: نرم‌افزار «کیف پول هوشمند» (Next.js + MySQL/PostgreSQL) — یک دفتر حساب شخصی.
- هدف: ساخت **نرم‌افزار حسابداری تحت وب** با قابلیت‌های بسیار بیشتر، در سه سطح: شخصی پیشرفته ← کسب‌وکار کوچک ← شرکتی.
- اسناد مرجع: `PRD.md` (نقشه راه، نسخه ۰.۲)، `DATA_MODEL.md` (طراحی مدل داده، نسخه ۰.۱)، `Agents.md` (قوانین کار ایجنت، نسخه ۲) و `WALLET_ISSUES.md` (چک‌لیست ایرادهای کیف پول — **در این مخزن موجود نیست**؛ بند ۸ را ببین).
- استقرار فعلی: GitHub + Vercel | آینده: سرور شخصی/ابری (کد باید قابل‌حمل بماند).

## ۲. تصمیم‌های تأییدشده کارفرما

| تاریخ | تصمیم |
|---|---|
| ۱۴۰۵/۰۶/۲۳ | بررسی اولیه بدون هیچ کدنویسی انجام شود |
| ۱۴۰۵/۰۶/۲۳ | مخاطب هدف: **ترکیبی از هر سه سطح** (شخصی + کسب‌وکار + شرکتی) |
| ۱۴۰۵/۰۶/۲۳ | قدم اول: تدوین PRD |
| ۱۴۰۵/۰۶/۲۳ | MVP = شخصی پیشرفته + پایه چندکاربره (فاکتور و انبار در فازهای بعد، ولی حتماً در نقشه راه) |
| ۱۴۰۵/۰۶/۲۳ | فعلاً تک‌مجموعه، ولی طراحی چندمجموعه‌پذیر از روز اول |
| ۱۴۰۵/۰۶/۲۳ | فعلاً فقط وب مدرن (بدون PWA آفلاین و بدون اپ نیتیو) |
| ۱۴۰۵/۰۶/۲۳ | استراتژی کد (ارتقا/بازنویسی): تصمیم‌گیری **بعد از طراحی مدل داده** |
| ۱۴۰۵/۰۶/۲۳ | ایجنت مستقیم روی گیت‌هاب پوش کند (روی برنچ سشن) + این فایل حافظه نگهداری شود |
| ۱۴۰۵/۰۶/۲۳ | فایل‌های موقت پس از پاس شدن تست هر مرحله **حذف شوند** (جلوگیری از کمبود حافظه/شلوغی) |
| ۱۴۰۵/۰۶/۲۳ | کدنویسی‌ها باید **تمیز و مرتب** باشند (قابل‌فهم برای ناظر بیرونی) |
| ۱۴۰۵/۰۶/۲۳ | نیاز آینده: **لینک خودکار کیف پول → نرم‌افزار جدید** برای انتقال خودکار بعضی تراکنش‌ها |
| ۱۴۰۵/۰۶/۲۳ | نیاز آینده: **سیستم تولید چند مرحله‌ای** (در طراحی مدل داده جای آن دیده شود) |
| ۱۴۰۵/۰۶/۲۳ | ارتقای PRD به نسخه ۰.۲ و تدوین سند «طراحی مدل داده» **با هم و در یک مرحله** انجام شود |
| ۱۴۰۵/۰۶/۲۳ | چهار تصمیم فنی PRD (سندمحوری، Postgres تک‌موتوره، مبالغ صحیح ریالی، نشست امن) به‌عنوان **پیش‌فرض کاری** در مدل داده اعمال شوند تا کار متوقف نماند؛ تأیید نهایی همچنان باز است |

## ۳. قوانین دائمی کار (Standing Rules)

1. **بدون کد تا تأیید اولیه:** تا کارفرما نتیجه اولیه (PRD + مدل داده) را تأیید نکرده، هیچ کدی نوشته نمی‌شود.
2. **برنچ سشن:** همه کارها فقط روی همین برنچ؛ کامیت‌های منظم با پیام فارسی؛ پوش فقط به همین برنچ؛ هیچ‌وقت پوش مستقیم به main.
3. **پاک‌سازی فایل موقت:** هر فایل/اسکریپت موقت (تست، بررسی، مهاجرت آزمایشی) بلافاصله پس از پاس شدن تست آن مرحله حذف می‌شود. خروجی‌های حجیم (دیتاست، بکاپ، لاگ) هرگز کامیت نمی‌شوند.
4. **کد تمیز:** ساختار ماژولار، نام‌گذاری معنادار، توابع کوچک تک‌مسئولیته، بدون کد مرده، مستندسازی تصمیم‌های غیربدیهی. هر قابلیت جدید باید برای یک توسعه‌دهنده بیرونی بدون توضیح شفاهی قابل‌فهم باشد.
5. **امنیت پیش‌فرض:** هیچ قابلیتی بدون اعمال دسترسی سمت سرور، اعتبارسنجی ورودی و تست، «تمام‌شده» محسوب نمی‌شود.
6. **به‌روزرسانی همین فایل** طبق قانون بالای صفحه (فراموش‌شدنی نیست).

## ۴. لاگ پیشرفت

| تاریخ | رویداد |
|---|---|
| ۱۴۰۵/۰۶/۲۳ | بررسی کامل مخزن کیف پول (APIها، دیتابیس، فرانت، پارسر پیامک، مستندات) انجام شد |
| ۱۴۰۵/۰۶/۲۳ | گزارش نقاط قوت (۱۰ مورد) و ضعف (امنیتی/حسابداری/فنی) به کارفرما ارائه شد |
| ۱۴۰۵/۰۶/۲۳ | پیش‌نویس `PRD.md` نسخه ۰.۱ نوشته و پوش شد — در انتظار تأیید |
| ۱۴۰۵/۰۶/۲۳ | `AGENT_WORKLOG.md` (همین فایل) و `WALLET_ISSUES.md` (چک‌لیست ایرادها) ساخته و پوش شد |
| ۱۴۰۵/۰۶/۲۳ | نیازهای جدید ثبت شد: لینک خودکار کیف پول + تولید چندمرحله‌ای (بک‌لاگ، اثر در PRD و مدل داده) |
| ۱۴۰۵/۰۶/۲۳ | کشف شد `main` بازنویسی شده (تک‌کامیت equity-sync) و تاریخچه‌ها نامرتبط‌اند؛ ۲ کامیت سندی روی آخرین `main` ری‌بیس و PR شماره ۸ باز شد |
| ۱۴۰۵/۰۶/۲۳ | بازبینی کامل مخزن `Desktop_Account` در محیط Arena: فقط ۳ فایل متنی، یک کامیت، بدون PR و بدون کد |
| ۱۴۰۵/۰۶/۲۳ | `PRD.md` به **نسخه ۰.۲** ارتقا یافت: بخش جدید «یکپارچه‌سازی و منابع خارجی» (لینک کیف پول)، اصل مکمل طراحی، درج تولید چندمرحله‌ای در فاز ۴+، ۱۵ موجودیت جدید در مدل مفهومی، ۲ نیازمندی غیرعملکردی جدید، ۳ ریسک جدید، قدم‌های بعدی و تاریخچهٔ نسخه‌ها |
| ۱۴۰۵/۰۶/۲۳ | `DATA_MODEL.md` **نسخه ۰.۱** نوشته و پوش شد: ۱۲ اصل طراحی، ~۳۵ موجودیت با فیلد/قید/ایندکس، ۷ تریگر تضمین تراز و تغییرناپذیری، استراتژی شماره‌گذاری بی‌شکاف، استراتژی دو لایهٔ مانده، ۱۲ سناریوی کلیدی با آرتیکل واقعی، نگاشت مهاجرت کیف پول، ماتریس ردیابی PRD، برنامهٔ تست و ۷ پرسش باز |
| ۱۴۰۵/۰۶/۲۳ | `Agents.md` به **نسخه ۲** به‌روز شد: محیط فعلی push/PR مستقیم دارد (پروتکل انتقال دستی کد به بخش یدکی منتقل شد) + قاعدهٔ CRLF + قاعدهٔ نام‌گذاری انگلیسی در کد + ارجاع به سندهای مرجع |

## ۵. در انتظار تصمیم/تأیید کارفرما (باز)

- [ ] تأیید **PRD نسخه ۰.۲** (یا اعلام اصلاحات)
- [ ] تأیید **`DATA_MODEL.md` نسخه ۰.۱** (یا اعلام اصلاحات)
- [ ] تأیید نهایی ۴ تصمیم فنی که به‌عنوان پیش‌فرض کاری اعمال شدند (سندمحوری، Postgres تک‌موتوره، مبالغ صحیح ریالی، نشست امن)
- [ ] **تصمیم استراتژی کد** (ارتقای مخزن کیف پول یا مخزن جدید) — نیازمند دسترسی به مخزن/اسکیمای کیف پول
- [ ] جزئیات لینک کیف پول (۵ پرسش در `PRD.md` بخش ۴.۳): یک‌طرفه/دوطرفه، کدام تراکنش‌ها، از چه تاریخی، مرجع مغایرت، محل استقرار کیف پول
- [ ] ۷ پرسش باز مدل داده (`DATA_MODEL.md` بخش ۱۶): واحد نمایش پیش‌فرض (ریال/تومان)، انعطاف سال مالی، سطح دسترسی حذف، ثبت خودکار پیامک، جهت لینک، دسترسی به کد قدیم، دامنهٔ چندزبانه/چندارزی

## ۶. بک‌لاگ نیازمندی‌های آینده (اثر در طراحی، نه در MVP)

1. **لینک خودکار کیف پول → نرم‌افزار جدید:** نیازمند API امن دریافت تراکنش (توکن)، شناسه یکتای ضدتکرار، و جدول نگاشت سرفصل‌ها. در PRD نسخه ۰.۲ به بخش یکپارچه‌سازی اضافه شود؛ در مدل داده، موجودیت «منبع خارجی تراکنش» از ابتدا دیده شود.
2. **سیستم تولید چند مرحله‌ای:** در فاز ۴+ همراه/بعد از انبار. در طراحی مدل داده، موجودیت‌های کالا/انبار طوری طراحی شوند که بعداً «فرمول تولید (BOM) و مراحل تولید» به آن‌ها متصل شود — بدون بازنویسی.

## ۷. وضعیت فعلی و قدم بعدی

- **وضعیت:** ⏳ هر دو سند تحویل داده شدند (PRD ۰.۲ + DATA_MODEL ۰.۱)؛ منتظر تأیید کارفرما و پاسخ پرسش‌های باز.
- **قدم بعدی پس از تأیید:** شروع **فاز ۰** — `schema.prisma` + مهاجرت‌های SQL (تریگرها، قیدها، RLS) + seed حساب‌های سیستمی + تست‌های طلایی موتور مالی (سناریوهای بخش ۱۰ `DATA_MODEL.md`).
- **مانع فعلی:** کد/اسکیمای «کیف پول هوشمند» در این مخزن نیست؛ تا دریافت آن، نگاشت مهاجرت (`DATA_MODEL.md` بند ۱۱) و تصمیم استراتژی کد قطعی نمی‌شود.
- **یادآوری:** دو نیاز «لینک کیف پول» و «تولید چندمرحله‌ای» در PRD نسخه ۰.۲ لحاظ شدند ✅ (دیگر باز نیستند).

---

## ۸. مغایرت‌های مشاهده‌شده بین اسناد و واقعیت مخزن

| مورد | انتظار طبق لاگ قبلی | واقعیت فعلی | اقدام |
|---|---|---|---|
| `WALLET_ISSUES.md` | ساخته و پوش شده | در مخزن `Desktop_Account` وجود ندارد | اگر لازم است، از مخزن کیف پول بازیابی یا از نو تدوین شود |
| PR شماره ۸ | باز شده بود | این مخزن هیچ PR ندارد (و فقط یک کامیت) | احتمالاً مربوط به مخزن دیگر بوده؛ در این مخزن PR جدید از برنچ نشست زده می‌شود |
| پروتکل انتقال دستی کد در `Agents.md` | «push از سندباکس ممکن نیست» | در محیط Arena `git` و `gh` کار می‌کنند | در `Agents.md` نسخه ۲ اصلاح شد: push مستقیم به برنچ نشست + PR؛ پروتکل دستی فقط یدکی |
| کد کیف پول | مبنای تحلیل PRD | در این مخزن نیست | درخواست دسترسی از کارفرما (بند ۷) |