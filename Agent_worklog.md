# لاگ کاری ایجنت — حافظه پروژه (AGENT WORKLOG)

> **هدف این فایل:** حافظه مکتوب ایجنت در ریشه پروژه. هر تصمیم مهم، هر قدم انجام‌شده و هر قانون دائمی اینجا ثبت می‌شود تا در طول پروژه (حتی اگر طولانی شود) چیزی گم نشود.
>
> **قانون به‌روزرسانی:** ایجنت موظف است این فایل را در این مواقع به‌روز کند:
> ۱) پایان هر قدم/مایلستون ۲) هر تصمیم جدید کارفرما ۳) هر تغییر مسیر فنی ۴) شروع و پایان هر فاز اجرایی.
> زبان مستندات: فارسی.

---

## ۱. زمینه پروژه (خلاصه)

- مبنا: نرم‌افزار «کیف پول هوشمند» (Next.js + MySQL/PostgreSQL) — یک دفتر حساب شخصی.
- هدف: ساخت **نرم‌افزار حسابداری تحت وب** با قابلیت‌های بسیار بیشتر، در سه سطح: شخصی پیشرفته ← کسب‌وکار کوچک ← شرکتی.
- اسناد مرجع: `PRD.md` (نقشه راه، نسخه **۰.۳ تأییدشده**)، `DATA_MODEL.md` (طراحی مدل داده، نسخه **۰.۲ تأییدشده**)، `Agents.md` (قوانین کار ایجنت، نسخه ۲)، `README.md` (نمای کلی) و `WALLET_ISSUES.md` (چک‌لیست ایرادهای کیف پول — **در این مخزن موجود نیست**؛ بند ۸ را ببین).
- استقرار فعلی: GitHub + Vercel | آینده: سرور شخصی/ابری (کد باید قابل‌حمل بماند).

## ۲. تصمیم‌های تأییدشده کارفرما

| تاریخ | تصمیم |
|---|---|
| ۱۴۰۵/۰۶/۲۳ | بررسی اولیه بدون هیچ کدنویسی انجام شود |
| ۱۴۰۵/۰۶/۲۳ | مخاطب هدف: **ترکیبی از هر سه سطح** (شخصی + کسب‌وکار + شرکتی) |
| ۱۴۰۵/۰۶/۲۳ | قدم اول: تدوین PRD |
| ۱۴۰۵/۰۶/۲۳ | MVP = شخصی پیشرفته + پایه چندکاربره (فاکتور و انبار در فازهای بعد، ولی حتماً در نقشه راه) |
| ۱۴۰۵/۰۶/۲۳ | فعلاً تک‌مجموعه، ولی طراحی چندمجموعه‌پذیر از روز اول |
| ۱۴۰۵/۰۶/۲۳ | فعلاً فقط وب مدرن (بدون PWA آفلاین و بدون اپ نیتیو) |
| ۱۴۰۵/۰۶/۲۳ | استراتژی کد (ارتقا/بازنویسی): تصمیم‌گیری **بعد از طراحی مدل داده** |
| ۱۴۰۵/۰۶/۲۳ | ایجنت مستقیم روی گیت‌هاب پوش کند (روی برنچ سشن) + این فایل حافظه نگهداری شود |
| ۱۴۰۵/۰۶/۲۳ | فایل‌های موقت پس از پاس شدن تست هر مرحله **حذف شوند** (جلوگیری از کمبود حافظه/شلوغی) |
| ۱۴۰۵/۰۶/۲۳ | کدنویسی‌ها باید **تمیز و مرتب** باشند (قابل‌فهم برای ناظر بیرونی) |
| ۱۴۰۵/۰۶/۲۳ | نیاز آینده: **لینک خودکار کیف پول → نرم‌افزار جدید** برای انتقال خودکار بعضی تراکنش‌ها |
| ۱۴۰۵/۰۶/۲۳ | نیاز آینده: **سیستم تولید چند مرحله‌ای** (در طراحی مدل داده جای آن دیده شود) |
| ۱۴۰۵/۰۶/۲۳ | ارتقای PRD به نسخه ۰.۲ و تدوین سند «طراحی مدل داده» **با هم و در یک مرحله** انجام شود |
| ۱۴۰۵/۰۶/۲۳ | چهار تصمیم فنی PRD (سندمحوری، Postgres تک‌موتوره، مبالغ صحیح ریالی، نشست امن) به‌عنوان **پیش‌فرض کاری** در مدل داده اعمال شوند تا کار متوقف نماند |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تأیید نهایی:** PRD و `DATA_MODEL.md` توسط کارفرما تأیید شدند → مانع «بدون کد تا تأیید» برداشته شد؛ **شروع فاز ۰ مجاز است** |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم:** واحد نمایش پیش‌فرض مبلغ = **ریال** (ذخیره همیشه ریالِ صحیح؛ `companies.base_unit` برای تغییر احتمالی به تومان) |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم:** لینک کیف پول هوشمند **یک‌طرفه** است (فقط دریافت تراکنش از کیف پول؛ هیچ نوشتنی روی کیف پول انجام نمی‌شود) |
| ۱۴۰۵/۰۶/۲۳ | کارفرما اعلام کرد دسترسی به کد/اسکیمای کیف پول را «هر جور ایجنت بگوید» فراهم می‌کند → روش پیشنهادی: آدرس مخزن گیت‌هاب (تلاش برای clone) یا کپی `prisma/schema.prisma` + `package.json` + یک بکاپ نمونهٔ JSON در پوشهٔ `legacy/` همین مخزن؛ **هرگز `.env` یا توکن ارسال نشود** |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم نوبت دوم:** هیچ تراکنش تاریخی از کیف پول وارد نمی‌شود؛ فقط داده‌های جدید پس از راه‌اندازی به‌صورت افزایشی منتقل می‌شود |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم نوبت دوم:** سال مالی باید قابل بسته شدن و گشایش سال جدید باشد (سند اختتامیه/افتتاحیه + قفل دوره) |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم نوبت دوم:** حذف و ویرایش تراکنش باید امکان‌پذیر باشد — پیاده‌سازی: OWNER/ACCOUNTANT مجاز، VIEWER محروم؛ در DB به‌صورت VOID+سند جدید با ممیزی |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم نوبت دوم:** ثبت خودکار پیامک نمی‌خواهد — همیشه با تأیید کاربر |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم نوبت دوم:** چندزبانه/چندارزی نمی‌خواهد |
| ۱۴۰۵/۰۶/۲۳ | ℹ️ **پرسش کارفرما:** «دسترسی به کد قدیم یعنی چی؟» — پاسخ داده شد (بند ۸ توضیح) + درخت فایل کیف پول (Next.js + Drizzle + pg + jalaali-js) دریافت شد |
| ۱۴۰۵/۰۶/۲۳ | دریافت و تحلیل کامل `src/db/schema.ts` کیف پول (۳ جدول: `users`, `accounts`, `transactions` با Drizzle ORM روی Postgres/MySQL) — نگاشت فیلدبه‌فیلد در `DATA_MODEL.md` بند ۱۱ قطعی شد |
| ۱۴۰۵/۰۶/۲۳ | ✅ **تصمیم قطعی کارفرما:** کدگذاری برای تمام موجودیت‌ها (حساب‌ها، اشخاص، بانک‌ها، اسناد، فاکتورها، چک‌ها و...) باید **۱۰۰٪ اتوماتیک** باشد |
| ۱۴۰۵/۰۶/۲۳ | ✅ **دستور کارفرما:** تقسیم کار به **مراحل کوچک و آزمون‌پذیر (Milestone breakdown)** برای پیشگیری از بروز باگ‌ها یا سردرگمی |

## ۳. قوانین دائمی کار (Standing Rules)

1. **بدون کد تا تأیید اولیه:** تا کارفرما نتیجه اولیه (PRD + مدل داده) را تأیید نکرده، هیچ کدی نوشته نمی‌شود.
2. **برنچ سشن:** همه کارها فقط روی همین برنچ؛ کامیت‌های منظم با پیام فارسی؛ پوش فقط به همین برنچ؛ هیچ‌وقت پوش مستقیم به main.
3. **پاک‌سازی فایل موقت:** هر فایل/اسکریپت موقت (تست، بررسی، مهاجرت آزمایشی) بلافاصله پس از پاس شدن تست آن مرحله حذف می‌شود. خروجی‌های حجیم (دیتاست، بکاپ، لاگ) هرگز کامیت نمی‌شوند.
4. **کد تمیز:** ساختار ماژولار، نام‌گذاری معنادار، توابع کوچک تک‌مسئولیته، بدون کد مرده، مستندسازی تصمیم‌های غیربدیهی. هر قابلیت جدید باید برای یک توسعه‌دهنده بیرونی بدون توضیح شفاهی قابل‌فهم باشد.
5. **امنیت پیش‌فرض:** هیچ قابلیتی بدون اعمال دسترسی سمت سرور، اعتبارسنجی ورودی و تست، «تمام‌شده» محسوب نمی‌شود.
6. **به‌روزرسانی همین فایل** طبق قانون بالای صفحه (فراموش‌شدنی نیست).

## ۴. لاگ پیشرفت

| تاریخ | رویداد |
|---|---|
| ۱۴۰۵/۰۶/۲۳ | بررسی کامل مخزن کیف پول (APIها، دیتابیس، فرانت، پارسر پیامک، مستندات) انجام شد |
| ۱۴۰۵/۰۶/۲۳ | گزارش نقاط قوت (۱۰ مورد) و ضعف (امنیتی/حسابداری/فنی) به کارفرما ارائه شد |
| ۱۴۰۵/۰۶/۲۳ | پیش‌نویس `PRD.md` نسخه ۰.۱ نوشته و پوش شد — در انتظار تأیید |
| ۱۴۰۵/۰۶/۲۳ | `AGENT_WORKLOG.md` (همین فایل) و `WALLET_ISSUES.md` (چک‌لیست ایرادها) ساخته و پوش شد |
| ۱۴۰۵/۰۶/۲۳ | نیازهای جدید ثبت شد: لینک خودکار کیف پول + تولید چندمرحله‌ای (بک‌لاگ، اثر در PRD و مدل داده) |
| ۱۴۰۵/۰۶/۲۳ | کشف شد `main` بازنویسی شده (تک‌کامیت equity-sync) و تاریخچه‌ها نامرتبط‌اند؛ ۲ کامیت سندی روی آخرین `main` ری‌بیس و PR شماره ۸ باز شد |
| ۱۴۰۵/۰۶/۲۳ | بازبینی کامل مخزن `Desktop_Account` در محیط Arena: فقط ۳ فایل متنی، یک کامیت، بدون PR و بدون کد |
| ۱۴۰۵/۰۶/۲۳ | `PRD.md` به **نسخه ۰.۲** ارتقا یافت: بخش جدید «یکپارچه‌سازی و منابع خارجی» (لینک کیف پول)، اصل مکمل طراحی، درج تولید چندمرحله‌ای در فاز ۴+، ۱۵ موجودیت جدید در مدل مفهومی، ۲ نیازمندی غیرعملکردی جدید، ۳ ریسک جدید، قدم‌های بعدی و تاریخچهٔ نسخه‌ها |
| ۱۴۰۵/۰۶/۲۳ | `DATA_MODEL.md` **نسخه ۰.۱** نوشته و پوش شد: ۱۲ اصل طراحی، ~۳۵ موجودیت با فیلد/قید/ایندکس، ۷ تریگر تضمین تراز و تغییرناپذیری، استراتژی شماره‌گذاری بی‌شکاف، استراتژی دو لایهٔ مانده، ۱۲ سناریوی کلیدی با آرتیکل واقعی، نگاشت مهاجرت کیف پول، ماتریس ردیابی PRD، برنامهٔ تست و ۷ پرسش باز |
| ۱۴۰۵/۰۶/۲۳ | `Agents.md` به **نسخه ۲** به‌روز شد: محیط فعلی push/PR مستقیم دارد (پروتکل انتقال دستی کد به بخش یدکی منتقل شد) + قاعدهٔ CRLF + قاعدهٔ نام‌گذاری انگلیسی در کد + ارجاع به سندهای مرجع |
| ۱۴۰۵/۰۶/۲۳ | `README.md` ساخته و PR شماره ۱ روی مخزن `Desktop_Account` باز شد (۵ فایل سندی) |
| ۱۴۰۵/۰۶/۲۳ | پس از تأیید کارفرما: `PRD.md` به **۰.۳** و `DATA_MODEL.md` به **۰.۲** ارتقا یافتند (ثبت تأییدها، قطعی‌شدن ریال و یک‌طرفه بودن لینک، به‌روزرسانی پرسش‌های باز و قدم‌های بعدی) |
| ۱۴۰۵/۰۶/۲۳ | **شروع فاز ۰:** `db/prisma/schema.prisma` نوشته و اعتبارسنجی شد (۲۰ مدل، ۲۹ enum، ۱۰۴ رابطهٔ دوطرفهٔ سازگار) — شامل احراز هویت/نقش‌ها، موتور سند، حساب‌ها، طرف‌حساب‌ها، سال و دورهٔ مالی، اسنپ‌شات مانده، شمارندهٔ شماره، پیوست، بکاپ/مهاجرت و یکپارچه‌سازی یک‌طرفهٔ کیف پول |
| ۱۴۰۵/۰۶/۲۳ | `db/sql/0001_core_constraints_and_triggers.sql` نوشته شد: ۳۰ قید CHECK، ۹ ایندکس (partial/functional)، ۹ تریگر (تضمین تراز، تغییرناپذیری سند، قفل دوره، درخت حساب و جلوگیری از حلقه، محافظت از حساب سیستمی/دارای گردش، `email_lower`)، ۴ تابع سرویس (`fn_next_serial` بی‌شکاف، `fn_apply_balance_delta`، `fn_rebuild_period_balances`، `fn_touch_updated_at`) و ۴ view |
| ۱۴۰۵/۰۶/۲۳ | اعتبارسنجی با **پارسر واقعی PostgreSQL** (`libpg-query` ۱۷): ۵۷ جمله بدون خطا + جمله‌های SQL داخل بدنهٔ توابع هم پاس شد؛ تست اجرایی روی Postgres واقعی به CI موکول شد (در سندباکس ممکن نیست) |
| ۱۴۰۵/۰۶/۲۳ | `.gitignore` اضافه شد (جلوگیری از کامیت شدن `.env`، `node_modules` و خروجی‌های حجیم طبق قانون ۳)؛ اسکریپت‌های موقت اعتبارسنجی فقط در `/tmp` ساخته شدند و **کامیت نشدند** |
| ۱۴۰۵/۰۶/۲۳ | **پاسخ‌های نوبت دوم دریافت شد:** ۵ تصمیم قطعی جدید + درخت فایل کیف پول؛ `PRD.md` به ۰.۴ و `DATA_MODEL.md` به ۰.۳ ارتقا یافتند (بخش‌های ۴.۳، ۶، ۷، ۱۰، ۱۶ و ۱۷)؛ ماتریس دسترسی ۳.۳ برای ویرایش/حذف به‌روزرسانی شد |
| ۱۴۰۵/۰۶/۲۳ | دریافت محتوای کامل `src/db/schema.ts`؛ ارتقای `PRD.md` به **۰.۵** و `DATA_MODEL.md` به **۰.۴** (قطعی‌کردن نگاشت ستون‌به‌ستون بند ۱۱ و درج اصل کدگذاری خودکار) |
| ۱۴۰۵/۰۶/۲۳ | **اجرای گام ۰.۱ و ۰.۲ فاز ۰:** ۱) افزودن `Counterparty.code` و `SerialKind` به `schema.prisma` ۲) نگارش توابع و تریگرهای کدگذاری خودکار در `0001_core_constraints_and_triggers.sql` ۳) ایجاد `db/seed/system_accounts.sql` با ساختار کامل کدینگ استاندارد ایران (گروه، کل، معین، تفصیلی) ۴) ایجاد `lib/jalali.ts` (الگوریتم بورکوفسکی + نرمال‌سازی یونیکد ی/ک + تبدیل ارقام فارسی) ۵) ایجاد و پاس شدن تست‌های طلایی سناریوهای مالی (`tests/financial_engine_scenarios.ts`) |
| ۱۴۰۵/۰۶/۲۳ | اعتبارسنجی مجدد با **پارسر واقعی PostgreSQL** (`libpg-query` ۱۷: ۶۳ جمله در 0001 و ۱ تابع در seed) و پارسر AST پریزما — تمام تست‌ها ۱۰۰٪ پاس شدند |

## ۵. در انتظار تصمیم/تأیید کارفرما (باز)

- [x] ~~تأیید PRD~~ → تأیید شد (۱۴۰۵/۰۶/۲۳)؛ نسخهٔ فعلی **۰.۴**
- [x] ~~تأیید `DATA_MODEL.md`~~ → تأیید شد (۱۴۰۵/۰۶/۲۳)؛ نسخهٔ فعلی **۰.۳**
- [x] ~~تأیید ۴ تصمیم فنی~~ → سندمحوری، Postgres تک‌موتوره، مبالغ صحیح ریالی و نشست امن تأیید شدند
- [x] ~~واحد نمایش مبلغ~~ → **ریال**
- [x] ~~جهت لینک کیف پول~~ → **یک‌طرفه + فقط افزایشی از روز فعال‌سازی** (نوبت دوم)
- [x] ~~سال مالی~~ → **قابل بستن و گشایش سال جدید** (نوبت دوم)
- [x] ~~حذف/ویرایش تراکنش~~ → **مجاز (OWNER/ACCOUNTANT)** با VOID+سند جدید (نوبت دوم)
- [x] ~~ثبت خودکار پیامک~~ → **نمی‌خواهد** (همیشه با تأیید)
- [x] ~~چندزبانه/چندارزی~~ → **نمی‌خواهد**
- [x] ~~بررسی اسکیمای کیف پول~~ → `src/db/schema.ts` دریافت و بررسی شد (Drizzle + pg/mysql2)؛ نگاشت بند ۱۱ قطعی شد
- [x] ~~کدگذاری خودکار~~ → الزامی و پیاده‌سازی شد (حساب‌ها، اشخاص، بانک‌ها، اسناد)
- [ ] **تصمیم استراتژی پیاده‌سازی کد (ردیف ۵ PRD):** آیا ماژول مالی جدید در همین مخزن `Desktop_Account` با Next.js مدرن کامل پیاده شود یا کد کیف پول روی آن سوار شود؟ (توصیه: ادامه در همین مخزن با ساختار Next.js App Router و لایه داده Prisma/Postgres)
- [ ] ۲ پرسش کم‌اهمیت باقی لینک کیف پول (`PRD.md` ۴.۳ موارد ۴ و ۵): مرجع مغایرت و محل استقرار کیف پول
- [ ] ۱ پرسش توضیحی باقی (`DATA_MODEL.md` ۱۶ مورد ۶): «دسترسی به کد قدیم یعنی چی» — توضیح داده شد، منتظر ارسال `src/db/schema.ts` + نمونه بکاپ JSON

## ۶. بک‌لاگ نیازمندی‌های آینده (اثر در طراحی، نه در MVP)

1. **لینک خودکار کیف پول → نرم‌افزار جدید:** نیازمند API امن دریافت تراکنش (توکن)، شناسه یکتای ضدتکرار، و جدول نگاشت سرفصل‌ها. در PRD نسخه ۰.۲ به بخش یکپارچه‌سازی اضافه شود؛ در مدل داده، موجودیت «منبع خارجی تراکنش» از ابتدا دیده شود.
2. **سیستم تولید چند مرحله‌ای:** در فاز ۴+ همراه/بعد از انبار. در طراحی مدل داده، موجودیت‌های کالا/انبار طوری طراحی شوند که بعداً «فرمول تولید (BOM) و مراحل تولید» به آن‌ها متصل شود — بدون بازنویسی.

## ۷. وضعیت فعلی و قدم بعدی

- **وضعیت:** ✅ فاز ۰ با موفقیت جلو رفت: اسناد به `PRD 0.5` و `DATA_MODEL 0.4` ارتقا یافتند؛ نگاشت کیف پول قطعی شد؛ لایه داده با کدگذاری خودکار، سید حساب‌های سیستمی، ماژول تقویم شمسی و تست‌های طلایی تکمیل و اعتبارسنجی شد.
- **انجام‌شده در فاز ۰ تا این لحظه:**
  ۱) `db/prisma/schema.prisma` (۲۰ مدل + ۲۹ enum با کد یکتای اشخاص و شمارنده‌ها)
  ۲) `db/sql/0001_core_constraints_and_triggers.sql` (۶۳ جمله: تریگرها، قیدها، توابع کدگذاری خودکار `fn_next_account_code` و `fn_next_counterparty_code` و `fn_next_serial`)
  ۳) `db/seed/system_accounts.sql` (درخت کدینگ استاندارد ایران و حساب‌های پیش‌فرض)
  ۴) `lib/jalali.ts` (الگوریتم بورکوفسکی + نرمال‌سازی یونیکد فارسی + فرمت‌دهی/پارس ارقام)
  ۵) `tests/financial_engine_scenarios.ts` (تست‌های طلایی سناریوهای ۱۰.۱ و ۱۰.۲ و منفی)
  ۶) اعتبارسنجی ۱۰۰٪ با پارسر واقعی PostgreSQL و AST پریزما
- **قدم بعدی:**
  گام بعدی: ساخت موجودیت‌های تکمیلی فاز ۱ در اسکیما (`db/sql/0002_phase1_entities.sql` شامل تراکنش‌های دوره‌ای، بودجه‌بندی ماهانه، وام و اقساط، پیامک و یادآورها).
- **تصمیم ایجنت (قابل وتو):** لایهٔ داده فعلاً **قابل‌حمل** در `db/` همین مخزن می‌ماند؛ پس از دیدن `schema.ts`، به ریشهٔ اپ Next.js منتقل می‌شود (بدون بازنویسی منطق مالی).
- **مانع فعلی (کاهش‌یافته):** نگاشت ستون‌به‌ستون مهاجرت فقط منتظر محتوای `src/db/schema.ts` + یک نمونه بکاپ JSON است (دیگر نیازی به حدس نیست).

---

## ۸. مغایرت‌های مشاهده‌شده بین اسناد و واقعیت مخزن

| مورد | انتظار طبق لاگ قبلی | واقعیت فعلی | اقدام |
|---|---|---|---|
| `WALLET_ISSUES.md` | ساخته و پوش شده | در مخزن `Desktop_Account` وجود ندارد | اگر لازم است، از مخزن کیف پول بازیابی یا از نو تدوین شود |
| PR شماره ۸ | باز شده بود | این مخزن هیچ PR ندارد (و فقط یک کامیت) | احتمالاً مربوط به مخزن دیگر بوده؛ در این مخزن PR جدید از برنچ نشست زده می‌شود |
| پروتکل انتقال دستی کد در `Agents.md` | «push از سندباکس ممکن نیست» | در محیط Arena `git` و `gh` کار می‌کنند | در `Agents.md` نسخه ۲ اصلاح شد: push مستقیم به برنچ نشست + PR؛ پروتکل دستی فقط یدکی |
| کد کیف پول | مبنای تحلیل PRD | در این مخزن نیست | درخواست دسترسی از کارفرما (بند ۷) |
| Postgres/Docker برای تست تریگرها | لازم برای تست واقعی موتور مالی | در سندباکس نصب نمی‌شود (`apt` و `binaries.prisma.sh` مسدود) | جایگزین فعلی: اعتبارسنجی با **پارسر واقعی PostgreSQL** (`libpg-query` ۱۷) + پارسر اسکیمای Prisma؛ تست اجرایی باید در CI انجام شود |
| `prisma validate` / `migrate dev` | اعتبارسنجی رسمی اسکیمای Prisma | CLI به باینری‌های `binaries.prisma.sh` نیاز دارد و مسدود است | اسکریپت بررسی سفارشی روی AST: سازگاری دوطرفهٔ هر ۱۰۴ رابطه، وجود ستون‌های ایندکس/یونیک، تعریف همهٔ enumها، غیرoptional بودن کلید اصلی → پاس شد |
| درخت فایل کیف پول | انتظار: اسکیمای Drizzle در `src/db/schema.ts` | در خروجی ارسالی فقط لیست فایل بود («no schema file») | درخواست ارسال محتوای `src/db/schema.ts` + نمونه بکاپ JSON (بند ۷) |