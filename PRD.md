# سند نیازمندی‌ها و نقشه راه محصول (PRD) — نرم‌افزار حسابداری تحت وب

| مشخصه | مقدار |
|---|---|
| نسخه سند | ۰.۴ (**تأییدشده** — به‌روزرسانی پس از پاسخ‌های ۱۴۰۵/۰۶/۲۳ نوبت دوم) |
| تاریخ | ۲۳ شهریور ۱۴۰۵ (2026-09-14) |
| مبنا | تحلیل نرم‌افزار «کیف پول هوشمند» نسخه ۲.۶.۳ + درخت فایل ارسالی کیف پول (Next.js + Drizzle + pg) |
| سند وابسته | `DATA_MODEL.md` نسخه ۰.۳ (طراحی مدل داده) |
| وضعیت | ✅ تأیید کارفرما در ۱۴۰۵/۰۶/۲۳ — مانع «بدون کد تا تأیید» برداشته شد؛ شروع فاز ۰ مجاز است — پاسخ‌های نوبت دوم در بخش‌های ۴.۳ و ۱۰ ثبت شد |
| تغییرات ۰.۲ | افزودن بخش «یکپارچه‌سازی و منابع خارجی» (لینک خودکار کیف پول)، درج «تولید چندمرحله‌ای» در نقشه راه، به‌روزرسانی وضعیت تصمیم‌ها، و اتصال سند به `DATA_MODEL.md` |
| تغییرات ۰.۳ | ثبت تأیید کارفرما روی سند و تصمیم‌های فنی ۱–۴؛ قطعی‌شدن **واحد نمایش = ریال** و **یک‌طرفه بودن لینک کیف پول**؛ به‌روزرسانی قدم‌های بعدی |
| تغییرات ۰.۴ | ثبت ۵ پاسخ قطعی جدید (۱۴۰۵/۰۶/۲۳ نوبت دوم): فقط داده‌های جدید کیف پول پس از راه‌اندازی منتقل می‌شود (بدون مهاجرت تاریخچه)، سال مالی قابل بستن/گشایش سال جدید، ویرایش/حذف تراکنش مجاز است، عدم نیاز به ثبت خودکار پیامک و عدم نیاز به چندزبانه/چندارزی؛ به‌روزرسانی بخش‌های ۳، ۴.۳، ۶، ۸ و ۱۰ |

---

## ۱. چشم‌انداز و اهداف

**چشم‌انداز:** ساخت یک نرم‌افزار حسابداری تحت وب فارسی و شمسی‌محور که از مدیریت مالی شخصی شروع می‌کند، کسب‌وکارهای کوچک را پوشش می‌دهد و تا سطح شرکتی چندکاربره رشد می‌کند — همه روی **یک موتور مالی یکپارچه**.

**اهداف کلان:**

1. جایگزینی دفترهای کاغذی و فایل‌های پراکنده با یک سیستم یکپارچه، امن و همیشه‌در‌دسترس
2. حفظ نقاط قوت نسخه فعلی (سادگی، شمسی‌اول بودن، ثبت از پیامک) و رفع ضعف‌های بحرانی آن (امنیت، مدل حسابداری، مقیاس‌پذیری)
3. معماری توسعه‌پذیر: هر فاز روی فاز قبلی سوار شود، بدون بازنویسی

**اصل طلایی طراحی (مهم‌ترین تصمیم این سند):**

> موتور مالی از روز اول «سندمحور» طراحی می‌شود. هر تراکنش ساده شخصی، از نظر سیستمی یک **سند دوآرتیکلی خودکار** است؛ فاکتور، چک و سند دستیِ آینده هم روی همین موتور می‌نشینند. کاربر شخصی فقط یک «نمای ساده» روی همین موتور می‌بیند و هرگز با پیچیدگی مواجه نمی‌شود.

این تصمیم یعنی: **یک مدل داده، سه سطح تجربه کاربری** — نه سه سیستم جدا.

**اصل مکمل (افزوده در نسخه ۰.۲):**

> «منبع تراکنش» یک شهروند درجه‌یک مدل داده است: ثبت دستی، پیامک بانکی، اجرای دوره‌ای، **لینک خودکار کیف پول هوشمند** و مهاجرت از نسخهٔ قدیم همگی از یک مسیرِ «منبع خارجی ← نگاشت سرفصل ← سند» عبور می‌کنند. بنابراین افزودن هر منبع جدید در آینده (بانک، فروشگاه اینترنتی، اپ موبایل) فقط یک **پیکربندی** است، نه یک ماژول جدید.

---

## ۲. مخاطبان (سه سطح)

| سطح | پرسونا | نیاز کلیدی | اولویت |
|---|---|---|---|
| ۱️⃣ شخصی پیشرفته | فرد/خانواده؛ فریلنسر | بودجه، اقساط و وام، تراکنش دوره‌ای، گزارش تحلیلی، ثبت سریع | **فاز MVP** |
| ۲️⃣ کسب‌وکار کوچک | فروشگاه، شرکت خدماتی کوچک، پیمانکار جزء | فاکتور خرید/فروش، انبار و کالا، چک، طرف‌حساب‌ها، ارزش افزوده | فاز ۲ |
| ۳️⃣ شرکتی | مدیر مالی، حسابدار، حسابرس | سند دستی، کدینگ کامل، سال مالی و بستن دوره، صورت‌های مالی، نقش‌ها و تأییدیه‌ها، حسابرسی | فاز ۳ |

---

## ۳. نقشه راه فازبندی

```
فاز ۰ (زیرساخت و امنیت) ──▶ فاز ۱ (MVP) ──▶ فاز ۲ (کسب‌وکار) ──▶ فاز ۳ (شرکتی) ──▶ فاز ۴+ (آینده)
   پیش‌نیاز همه              شخصی پیشرفته     فاکتور/انبار/چک      سند/کدینگ/سال مالی   تولید چندمرحله‌ای،
   + پایه چندکاربره          + چندکاربره      + لینک خودکار کیف پول                    چندارزی، حقوق، موبایل
```

> نکته: **مدل دادهٔ** لینک کیف پول (منبع خارجی، شناسهٔ ضدتکرار، نگاشت سرفصل) از فاز ۰ در اسکیما وجود دارد تا افزودنش در فاز ۲ بدون مهاجرت سنگین ممکن باشد؛ **پیاده‌سازی** API آن در فاز ۲ است. جزئیات در `DATA_MODEL.md` بخش ۶.

### فاز ۰ — زیرساخت، امنیت و موتور مالی (پیش‌نیاز، بدون آن MVP شروع نمی‌شود)

- [ ] احراز هویت واقعی: ثبت‌نام/ورود، نشست امن (کوکی httpOnly)، خروج، فراموشی رمز (حداقلی)
- [ ] نقش‌های پایه: **مالک، حسابدار، مشاهده‌گر** (فعال در MVP؛ نقش‌های پیشرفته در فاز ۳)
- [ ] امنیت: هش مدرن رمز (Argon2 یا bcrypt با salt یکتا)، محدودیت نرخ ورود، قفل پس از تلاش ناموفق، لاگ حسابرسی پایه
- [ ] موتور سند یکپارچه: سند + آرتیکل‌های بدهکار/بستانکار (تراز بودن اجباری در سطح دیتابیس)
- [ ] مبالغ به‌صورت **عدد صحیح ریالی** (نه اعشار شناور)
- [ ] اسکیمای **چندمجموعه‌پذیر**: ستون مجموعه (`company_id`) روی همه جداول مالی + یک مجموعه پیش‌فرض (تک‌مجموعه فعال، چندمجموعه آماده)
- [ ] موجودیت‌های «منبع خارجی تراکنش» و «نگاشت سرفصل» در اسکیما (بدون پیاده‌سازی API) — پیش‌نیاز لینک خودکار کیف پول در فاز ۲
- [ ] چارچوب تست خودکار برای موتور مالی + CI روی گیت‌هاب
- [ ] استقرار: GitHub + Vercel با دیتابیس ابری (Supabase/Neon)؛ قابل‌انتقال به VPS در آینده بدون تغییر کد

### فاز ۱ — MVP: شخصی پیشرفته + چندکاربره ✅ (دامنه توافق‌شده)

**مدیریت حساب‌ها و سرفصل‌ها:**

- [ ] بانک، صندوق، شخص، سرفصل درآمد/هزینه؛ درخت چندسطحی در اسکیما (نمایش دو سطح در MVP)
- [ ] علاقه‌مندی‌ها، آیکون/رنگ، موجودی اولیه، جلوگیری از نام تکراری، محافظت از حذف دارای گردش

**تراکنش:**

- [ ] ثبت ساده مبدا ← مقصد (سیستم نوع را تشخیص می‌دهد و سند دوآرتیکلی می‌سازد)
- [ ] کارمزد، شرح، کد پیگیری، تاریخ شمسی، ویرایش/حذف با کنترل دسترسی نقش‌ها
- [ ] **تراکنش‌های دوره‌ای** (حقوق، اجاره، قسط...) با اجرای خودکار و تاریخچه
- [ ] **ثبت از پیامک بانکی** (حفظ قابلیت فعلی + بهبود تدریجی پارسر)

**بودجه، اقساط و پیگیری:**

- [ ] **بودجه‌بندی ماهانه** سرفصل‌ها + هشدار نزدیک‌شدن/عبور از سقف
- [ ] **وام و اقساط**: تعریف وام، جدول اقساط، ثبت پرداخت قسط، یادآور سررسید

**گزارش و گردش:**

- [ ] گردش هر حساب با مانده لحظه‌ای **مقیاس‌پذیر** (محاسبه در دیتابیس، نه حافظه)
- [ ] گزارش سرفصل‌ها، خلاصه دوره، **نمودار روند ماهانه** درآمد/هزینه
- [ ] خروجی **Excel و PDF** برای گردش و گزارش‌ها + چاپ تمیز
- [ ] جستجوی یکپارچه در حساب‌ها و تراکنش‌ها

**داده و اطمینان:**

- [ ] بکاپ JSON + اسنپ‌شات دوره‌ای + بازیابی امن (تراکنشی، بدون ریسک از دست رفتن داده)
- [ ] **ابزار مهاجرت از نسخه قدیم**: ورود فایل بکاپ «کیف پول هوشمند» به سیستم جدید

**رابط کاربری MVP:** وب مدرن ریسپانسیو (دسکتاپ‌اول، سازگار موبایل)، فارسی/راست‌چین، شمسی‌اول، داشبورد خلاصه.

**معیار پذیرش MVP (تعریف «تمام‌شده»):**

1. سه نقش کاربری با دسترسی‌های متفاوت واقعاً اعمال می‌شوند (تست نفوذ پایه پاس شود)
2. ۱۰۰٪ منطق موتور مالی (تراز سند، مانده، کارمزد) زیر پوشش تست خودکار است
3. گردش حسابی با ۱۰۰ هزار تراکنش در زیر ۲ ثانیه باز شود
4. مهاجرت کامل داده نسخه قدیم بدون خطا انجام شود

### فاز ۲ — کسب‌وکار کوچک (پس از MVP)

- [ ] **فاکتور فروش/خرید**: سریال خودکار، اقلام چندسطر‌ی، تخفیف سطری/کلی، مالیات بر ارزش افزوده، تسویه کامل/نسیه، چاپ فاکتور رسمی
- [ ] **کالا و انبار**: تعریف کالا/خدمت، کاردکس، رسید/حواله انبار، موجودی لحظه‌ای و هشدار نقطه سفارش
- [ ] **اشخاص تجاری**: سقف اعتبار، صورت‌حساب دوره‌ای، یادآور بدهی/طلب، تفکیک شخص حقیقی/حقوقی
- [ ] **چک**: دریافتنی/پرداختنی، سررسید، وضعیت (نزد صندوق/وصول‌شده/برگشتی)، یادآور سررسید
- [ ] گزارش‌های فروش، سود ناخالص دوره، مانده انبار
- [ ] **لینک خودکار کیف پول هوشمند ← سیستم جدید**: API امن با توکن، کلید ضدتکرار، نگاشت خودکار سرفصل‌ها (قاعده‌محور + یادگیری از انتخاب دستی کاربر)، صف بازبینی، مغایرت‌گیری مانده
- [ ] ورود دسته‌ای اطلاعات پایه از Excel

### فاز ۳ — شرکتی و چندمجموعه‌ای

- [ ] **سند دستی چندسطر‌ی** + الگوهای سند پرتکرار + کپی/حذف گروهی
- [ ] **کدینگ کامل حساب‌ها**: گروه ← کل ← معین ← تفصیلی (با امکان تفصیلی شناور)
- [ ] **سال مالی**: تعریف دوره، سند افتتاحیه/اختتامیه، **بستن سال مالی و گشایش سال جدید** (الزام قطعی ۱۴۰۵/۰۶/۲۳ نوبت دوم)، بستن موقت/دائم، **قفل دوره** (عدم امکان ثبت/ویرایش در دوره بسته — جزئیات قفل در `DATA_MODEL.md` بند ۴.۶)
- [ ] **صورت‌های مالی**: تراز آزمایشی (۲/۴/۶/۸ ستونی)، سود و زیان، ترازنامه، صورت جریان نقد
- [ ] فعال‌سازی **چندمجموعه‌ای**: چند شرکت/شعبه جدا با دسترسی تفکیک‌شده
- [ ] گردش تأیید اسناد (ثبت ← تأیید)، نقش‌های سفارشی با سطح دسترسی دانه‌ای
- [ ] API خواندن/نوشتن + وب‌هوک رویدادها (اتصال به فروشگاه اینترنتی و...)

### فاز ۴+ — آینده (فعلاً خارج از برنامه اجرایی)

**تولید چندمرحله‌ای** (فرمول تولید/BOM، مراحل تولید، کالای در جریان ساخت، بهای تمام‌شدهٔ تولید)، دارایی ثابت و استهلاک، چندارزی، حقوق و دستمزد، تنخواه‌گردان پیشرفته، اپ موبایل نیتیو، اتصال مستقیم بانکی، دسته‌بندی هوشمند با هوش مصنوعی.

> **الزام طراحی برای تولید چندمرحله‌ای (تصمیم کارفرما):** موجودیت‌های کالا/انبار از فاز ۲ طوری طراحی می‌شوند که «فرمول تولید و مراحل تولید» بعداً **بدون بازنویسی** به آن‌ها وصل شوند. این موضوع در `DATA_MODEL.md` با `products.type = MATERIAL/WIP`، انواع حرکت انبار `PRODUCTION_IN/PRODUCTION_OUT` و ارجاع `ref_type='PRODUCTION_ORDER'` از پیش دیده شده است (بخش ۹).

---

## ۴. یکپارچه‌سازی و منابع خارجی (افزوده در نسخه ۰.۲)

**نیاز ثبت‌شدهٔ کارفرما:** لینک خودکار «کیف پول هوشمند» به نرم‌افزار جدید برای انتقال خودکار بعضی تراکنش‌ها.

### ۴.۱ اصول طراحی یکپارچه‌سازی

| اصل | توضیح |
|---|---|
| امنیت با توکن | هر منبع خارجی یک توکن **هش‌شده**، دامنهٔ دسترسی (scope)، محدودیت نرخ و محدودیت IP دارد؛ توکن خام هرگز ذخیره یا لاگ نمی‌شود |
| ضدتکرار (idempotency) | هر تراکنش خارجی کلید یکتا دارد؛ ارسال دوبارهٔ همان تراکنش سند دوم نمی‌سازد و همان نتیجهٔ قبلی برمی‌گردد |
| نگاشت قابل‌ویرایش | قواعد نگاشت (کلیدواژه/فرستنده/حساب مبدأ/بازهٔ مبلغ) تعیین می‌کنند تراکنش به کدام سرفصل برود؛ انتخاب دستی کاربر به قاعدهٔ جدید تبدیل می‌شود (یادگیری تدریجی، بدون هوش مصنوعی) |
| صف بازبینی | تراکنشی که با اطمینان نگاشت نشود **سند نمی‌سازد** و در صف بازبینی کاربر قرار می‌گیرد |
| فقط‌افزودنی | منبع خارجی امکان ویرایش یا حذف سند ثبت‌شده را ندارد (اصل تغییرناپذیری اسناد) |
| منبع حقیقت | payload خام دریافتی همیشه نگه داشته می‌شود تا هر سند قابل ریشه‌یابی باشد |

### ۴.۲ دامنهٔ اجرایی در فازها

| فاز | چه چیزی ساخته می‌شود |
|---|---|
| فاز ۰ | موجودیت‌های اسکیما: منبع خارجی، تراکنش خارجی، قواعد نگاشت (بدون API) |
| فاز ۱ | انتقال دادهٔ کیف پول از طریق **ابزار مهاجرت** (ورود فایل بکاپ) |
| فاز ۲ | **API زندهٔ دریافت تراکنش** + صف بازبینی + مدیریت قواعد نگاشت در UI |
| فاز ۳ | API خواندن/نوشتن عمومی + وب‌هوک رویدادها (فروشگاه اینترنتی و سایر منابع) |

### ۴.۳ پرسش‌های این بخش — وضعیت (به‌روزرسانی ۰.۴)

1. ✅ **پاسخ (۱۴۰۵/۰۶/۲۳): یک‌طرفه** — فقط دریافت تراکنش از کیف پول به سیستم جدید. نوشتن تراکنش در کیف پول خارج از دامنه است (و با اصل «فقط‌افزودنی» هم می‌خواند). در نتیجه `external_sources.direction` در MVP همیشه `INBOUND` است.
2. ✅ **پاسخ (۱۴۰۵/۰۶/۲۳ نوبت دوم): فقط داده‌های جدید پس از راه‌اندازی** — هیچ تراکنش تاریخی از کیف پول لازم نیست وارد شود؛ پس از ران شدن نرم‌افزار جدید، فقط تراکنش‌های جدیدی که در کیف پول ثبت می‌شوند به‌صورت خودکار منتقل می‌شوند (انتقال افزایشی/تدریجی، نه مهاجرت یک‌باره). ابزار مهاجرت تاریخچه (بخش ۴.۲ فاز ۱) همچنان به‌عنوان گزینهٔ یدکی در اسکیما می‌ماند ولی در استقرار اولیه استفاده نمی‌شود.
3. ✅ **پاسخ (۱۴۰۵/۰۶/۲۳ نوبت دوم): از روز فعال‌سازی به بعد** — مبدأ زمانی = زمان فعال‌سازی لینک (یا `external_sources.created_at` برای هر مجموعه). تراکنش‌های قبل از آن نادیده گرفته می‌شوند.
4. ⏳ باز — در صورت مغایرت ماندهٔ کیف پول با سیستم جدید، مرجع کدام است؟ (با توجه به پاسخ ۲ و ۳، مغایرت تاریخچه منتفی است؛ فقط مغایرت تراکنش‌های افزایشی پس از فعال‌سازی مطرح است — پیشنهاد: سیستم جدید مرجع است و گزارش مغایرت روزانه تولید می‌شود)
5. ⏳ باز — کیف پول روی همان دیتابیس بماند یا مستقل؟ (اثر در استراتژی استقرار؛ با لینک افزایشی، استقرار مستقل با API توکن‌دار پیشنهاد می‌شود)

> جزئیات فنی (جداول، قیدها، ایندکس‌ها و قرارداد API) در `DATA_MODEL.md` بخش ۶ آمده است.

## ۵. مدل مفهومی داده (در حد مفهوم — جزئیات در سند طراحی مدل داده)

موجودیت‌های اصلی موتور یکپارچه:

| موجودیت | نقش | نکته کلیدی |
|---|---|---|
| `Company` (مجموعه) | تفکیک داده شرکت‌ها/شعبه‌ها | از روز اول در اسکیما؛ فعال‌سازی چندتایی در فاز ۳ |
| `User` + `Role` + `Membership` | کاربران، نقش‌ها، عضویت در مجموعه | هر کاربر می‌تواند در چند مجموعه نقش متفاوت داشته باشد |
| `Account` (حساب/سرفصل) | درخت حساب‌ها | چندسطحی؛ کدینگ کامل در فاز ۳ اضافه می‌شود |
| `Journal` (سند) | سربرگ سند مالی | شماره سریال یکتا، تاریخ، وضعیت (پیش‌نویس/ثبت‌شده)، دوره مالی |
| `JournalLine` (آرتیکل) | سطرهای بدهکار/بستانکار | جمع بدهکار = جمع بستانکار (اجباری)؛ تراکنش ساده = سند ۲ آرتیکلی |
| `Counterparty` (طرف‌حساب) | اشخاص حقیقی/حقوقی | مشخصات، سقف اعتبار، پیوند به حساب معین |
| `FiscalYear/Period` (دوره مالی) | سال و دوره‌های مالی | قفل دوره در فاز ۳ |
| `AuditLog` | ردپای تغییرات | چه کسی، کی، چه چیزی، قبل/بعد |
| `Document` (پیوست) | رسید و مدرک | فاز ۱ حداقلی ← تکمیل در فاز ۲ |
| `ExternalSource` + `ExternalTransaction` | منبع خارجی و تراکنش دریافتی از آن | کلید ضدتکرار؛ payload خام تغییرناپذیر؛ اسکیمای آن از فاز ۰ |
| `AccountMappingRule` | قواعد نگاشت تراکنش خارجی به سرفصل | اولویت‌دار؛ انتخاب دستی کاربر ← قاعدهٔ جدید |
| `SmsMessage` | پیامک بانکی خام + نتیجهٔ پارسر | هش ضدتکرار، نسخهٔ پارسر، درجهٔ اطمینان، صف تأیید |
| `RecurringRule` + `RecurringRun` | تراکنش دوره‌ای و تاریخچهٔ اجرا | `UNIQUE(rule, run_for_date)` جلوی اجرای دوباره را می‌گیرد |
| `Budget` + `BudgetLine` | بودجهٔ ماهانه و سقف هر سرفصل | مصرف‌شده **محاسبه** می‌شود نه ذخیره |
| `Loan` + `LoanInstallment` | وام و جدول اقساط | اقساط از پیش ساخته می‌شوند تا معوقه/بخشودگی ممکن باشد |
| `AccountPeriodBalance` | اسنپ‌شات ماندهٔ هر حساب در هر دوره | راز «گزارش زیر ۲ ثانیه»؛ منبع حقیقت = آرتیکل‌ها |
| `SerialCounter` | شمارندهٔ شمارهٔ اسناد/فاکتور/چک | شماره‌گذاری **بی‌شکاف** با قفل سطری در همان تراکنش |
| `ImportJob` + `BackupSnapshot` | مهاجرت، ورود Excel، بکاپ و بازیابی | `batch_tag` برای رول‌بک دقیق + مغایرت‌گیری مانده |
| `Reminder` | یادآور قسط، چک، هشدار بودجه | عمومی و چندمنظوره |
| `Product` / `Warehouse` / `StockMove` | کالا، انبار و کاردکس (فاز ۲) | آمادهٔ اتصال به تولید چندمرحله‌ای در فاز ۴ |
| `Invoice` + `InvoiceLine` + `Settlement` | فاکتور و تسویه (فاز ۲) | هر فاکتور مولد سند است، نه یک مدل موازی |
| `Check` + `CheckEvent` | چک دریافتنی/پرداختنی و تاریخچهٔ وضعیت (فاز ۲) | تغییر وضعیت = یک سند + یک رویداد |
| `Bom` + `ProductionOrder` | فرمول تولید و سفارش تولید (فاز ۴) | فقط افزودن جدول جدید، بدون تغییر جداول فاز ۲ |

> ✅ جزئیات فیلدها، روابط، قواعد یکپارچگی، تریگرهای تضمین تراز، استراتژی شماره‌گذاری و مانده‌ها، و سناریوهای کلیدی (ثبت ساده، پیامک، لینک کیف پول، فاکتور، چک، بستن دوره، تولید) در **`DATA_MODEL.md` نسخه ۰.۱** نوشته شده است — این جدول فقط نمای مفهومی است.

---

## ۶. نیازمندی‌های غیرعملکردی

| حوزه | نیازمندی |
|---|---|
| امنیت | نشست امن سمت سرور؛ اعمال دسترسی در **همه** APIها؛ هش مدرن؛ rate-limit؛ لاگ حسابرسی؛ بدون اطلاعات حساس در لاگ و بکاپ |
| عملکرد | گردش و گزارش‌ها با صفحه‌بندی و تجمیع دیتابیسی؛ هدف: پاسخ زیر ۲ ثانیه تا ۱۰۰ هزار سند در هر مجموعه |
| مقیاس‌پذیری | بدون خوانش کامل جدول در حافظه؛ ایندکس‌گذاری از روز اول؛ آماده انتقال Vercel ← VPS |
| کیفیت | تست خودکار موتور مالی (پوشش کامل منطق تراز/مانده)؛ CI؛ بازبینی امنیتی پیش از هر انتشار |
| استقرار | فاز ۰–۲: GitHub + Vercel + دیتابیس ابری؛ طراحی قابل‌حمل برای سرور شخصی/ابری در آینده |
| تجربه کاربری | فارسی، راست‌چین، شمسی‌اول؛ ریسپانسیو؛ خطاهای فارسی قابل‌فهم؛ بدون نیاز به دانش حسابداری در سطح شخصی؛ **واحد نمایش پیش‌فرض مبلغ: ریال** (تصمیم ۱۴۰۵/۰۶/۲۳؛ ذخیره همیشه ریالِ صحیح) |
| داده | مهاجرت بدون‌خطا از نسخه قدیم؛ بکاپ قابل‌بازیابی؛ ویرایش/حذف تراکنش **مجاز است** (۱۴۰۵/۰۶/۲۳ نوبت دوم) — پیاده‌سازی: ویرایش/حذف در UI مجاز و در سطح دیتابیس به‌صورت «ابطال (VOID) + سند جدید» با ثبت در `audit_logs` انجام می‌شود تا تراز و حسابرسی حفظ شود؛ حذف فیزیکی اسناد `POSTED` هرگز انجام نمی‌شود |
| یکپارچه‌سازی | توکن هش‌شده، ضدتکرار قطعی در سطح دیتابیس، محدودیت نرخ، صف بازبینی، نگه‌داشتن payload خام، و عدم امکان ویرایش/حذف از راه دور |
| حسابرسی‌پذیری | هر سند ثبت‌شده باید به منبعش (دستی/پیامک/دوره‌ای/خارجی/مهاجرت) قابل ریشه‌یابی باشد |

---

## ۷. صراحتاً خارج از دامنه (تا پایان فاز ۳)

اپ موبایل نیتیو • چندارزی و چندزبانه (**تأیید عدم نیاز ۱۴۰۵/۰۶/۲۳ نوبت دوم**) • حقوق و دستمزد • دارایی ثابت • تولید و بهای تمام‌شده • اتصال مستقیم به بانک/درگاه پرداخت • آفلاین کامل (طراحی وب‌محور است) • ثبت خودکار پیامک بدون تأیید کاربر (**تأیید عدم نیاز ۱۴۰۵/۰۶/۲۳**)

---

## ۸. تصمیم‌های باز + پیشنهاد فنی

> تصمیم‌های ۱ تا ۴ در تاریخ ۱۴۰۵/۰۶/۲۳ توسط کارفرما **تأیید شدند** و `DATA_MODEL.md` بر همین مبنا قطعی است. فقط ردیف ۵ (استراتژی کد) باز است و به دسترسی به مخزن «کیف پول هوشمند» وابسته است.

| # | تصمیم | پیشنهاد | وضعیت |
|---|---|---|---|
| ۱ | موتور سند یکپارچه از روز اول؟ | ✅ بله (اصل طلایی بخش ۱) | ✅ **تأیید شد** (۱۴۰۵/۰۶/۲۳) — در `DATA_MODEL.md` اعمال شده |
| ۲ | پایگاه داده: تک‌موتوره Postgres + ORM کامل، به‌جای لایه دستی دوگانه؟ (چون استقرار روی Vercel/Supabase و بعد VPS است، نیازی به MySQL دایرکت‌ادمین نیست) | ✅ بله | ✅ **تأیید شد** (۱۴۰۵/۰۶/۲۳) |
| ۳ | مبالغ: عدد صحیح ریالی (BIGINT)؟ | ✅ بله | ✅ **تأیید شد** (۱۴۰۵/۰۶/۲۳) |
| ۴ | احراز هویت: نشست امن سمت سرور + نقش‌ها (بدون OAuth در MVP)؟ | ✅ بله | ✅ **تأیید شد** (۱۴۰۵/۰۶/۲۳) |
| ۵ | استراتژی کد: (الف) همین مخزن — ماژول مالی جدید کنار کد فعلی + مهاجرت تدریجی (ب) مخزن جدید تمیز و انتقال تدریجی قابلیت‌ها | الف (ریسک کمتر، حفظ پیامک/شمسی/بکاپ) | ⏳ باز — **نیازمند دسترسی به مخزن/اسکیمای کیف پول**؛ مدل داده آماده است |

---

## ۹. ریسک‌ها

| ریسک | اثر | راه کاهش |
|---|---|---|
| بزرگ‌شدن دامنه (ترکیب هر ۳ سطح هم‌زمان) | تأخیر و فرسودگی | فازبندی سفت‌وسخت همین سند؛ هیچ قابلیت فاز ۲ وارد MVP نمی‌شود |
| مهاجرت داده از مدل ساده به سندمحور | خطای مانده‌ها | ابزار مهاجرت + مغایرت‌گیری خودکار مانده قبل/بعد + تست |
| امنیت (با توجه به سابقه نسخه فعلی) | نشت داده مالی | فاز ۰ مستقل؛ بازبینی امنیتی پیش از انتشار MVP |
| تغییر مسیر استقرار (Vercel ← VPS) | دوباره‌کاری | انتزاع لایه استقرار از روز اول؛ بدون وابستگی اختصاصی Vercel در کد مالی |
| تراکنش تکراری یا نگاشت اشتباه در لینک کیف پول | ماندهٔ غلط و از دست رفتن اعتماد کاربر | کلید ضدتکرار در سطح دیتابیس + صف بازبینی + نگه‌داشتن payload خام + مغایرت‌گیری ماندهٔ روزانه |
| نبودِ دسترسی به کد/اسکیمای نسخهٔ قدیم در این مخزن | ناتوانی در قطعی‌کردن نگاشت مهاجرت و تصمیم استراتژی کد | درخواست مخزن یا یک فایل بکاپ نمونه از کارفرما؛ تا آن زمان نگاشت مهاجرت «پیشنهادی» می‌ماند (`DATA_MODEL.md` بند ۱۱) |
| سنگین‌شدن مدل داده پیش از تأیید | دوباره‌کاری اگر پیش‌فرض‌ها رد شوند | بخش ۱۳ `DATA_MODEL.md` اثر هر تغییر تصمیم را جداگانه فهرست کرده است |

---

## ۱۰. قدم‌های بعدی توافق‌شده

1. ✅ **انجام شد (۱۴۰۵/۰۶/۲۳):** تأیید این سند و `DATA_MODEL.md` توسط کارفرما
2. ✅ **تکمیل شد (۱۴۰۵/۰۶/۲۳ نوبت دوم):** پاسخ به ۵ پرسش باز — واحد نمایش = **ریال** ✅، جهت لینک کیف پول = **یک‌طرفه** ✅، انتقال فقط افزایشی از روز فعال‌سازی ✅، سال مالی قابل بستن/گشایش ✅، ویرایش/حذف مجاز ✅، عدم نیاز به ثبت خودکار پیامک ✅، عدم نیاز به چندزبانه/چندارزی ✅؛ باقی فقط ۲ مورد ۴.۳ (مرجع مغایرت + محل استقرار) به‌صورت کم‌اهمیت باز است
3. 🔄 **نیمه‌انجام:** دسترسی به کد کیف پول — درخت فایل + `package.json` دریافت شد (Next.js + Drizzle + pg)؛ برای قطعی‌کردن نگاشت مهاجرت هنوز **محتوای `src/db/schema.ts` + یک نمونه بکاپ JSON** لازم است (توضیح «دسترسی به کد قدیم یعنی چی» در `Agent_worklog.md` بند ۸)
4. ⏳ **باز:** تصمیم استراتژی کد (ردیف ۵ جدول بالا) — با توجه به دریافت درخت فایل، گزینهٔ **الف (ارتقای همین مخزن Next.js)** محتمل‌تر شد؛ نهایی‌سازی پس از دیدن `schema.ts`
5. ✅ **مجاز شد و در جریان:** **فاز ۰** — `schema.prisma` + مهاجرت‌های SQL (تریگرها و قیدها) + seed حساب‌های سیستمی + تست‌های طلایی موتور مالی (وضعیت: اسکیما و تریگرها ساخته و اعتبارسنجی شد؛ قدم بعدی `db/seed/system_accounts.sql` + `lib/jalali.ts`)

---

## ۱۱. تاریخچهٔ نسخه‌ها

| نسخه | تاریخ | تغییرات |
|---|---|---|
| ۰.۱ | ۱۴۰۵/۰۶/۲۳ | پیش‌نویس اولیه: چشم‌انداز، فازبندی ۰ تا ۴، مدل مفهومی، نیازمندی‌های غیرعملکردی، تصمیم‌های باز و ریسک‌ها |
| ۰.۲ | ۱۴۰۵/۰۶/۲۳ | افزودن بخش ۴ «یکپارچه‌سازی و منابع خارجی» و «اصل مکمل» در بخش ۱؛ درج «تولید چندمرحله‌ای» در فاز ۴+ با الزام طراحی از فاز ۲؛ افزودن ۱۵ موجودیت به مدل مفهومی؛ دو نیازمندی غیرعملکردی جدید؛ ۳ ریسک جدید؛ بازنویسی قدم‌های بعدی؛ افزودن تاریخچهٔ نسخه‌ها |
| ۰.۳ | ۱۴۰۵/۰۶/۲۳ | ثبت **تأیید کارفرما** روی سند و تصمیم‌های فنی ۱–۴؛ قطعی‌شدن واحد نمایش (ریال) و یک‌طرفه بودن لینک کیف پول؛ برداشته‌شدن مانع «بدون کد تا تأیید»؛ به‌روزرسانی قدم‌های بعدی با وضعیت هر مورد |
| ۰.۴ | ۱۴۰۵/۰۶/۲۳ | ثبت **۵ پاسخ قطعی نوبت دوم**: انتقال افزایشی فقط پس از راه‌اندازی، سال مالی قابل بستن/گشایش، مجاز بودن ویرایش/حذف (با ابطال+سند جدید)، عدم نیاز به ثبت خودکار پیامک و عدم نیاز به چندزبانه/چندارزی؛ به‌روزرسانی بخش‌های ۴.۳، ۶ (NFR داده)، ۷، ۸ و ۱۰؛ دریافت درخت فایل کیف پول |

---

*پایان نسخه ۰.۴ — تأییدشده؛ مبنای ادامهٔ فاز ۰.*