# AGENTS.md — قوانین کار روی این ریپو (Arena Wallet)

این فایل برای ایجنت‌ها (نشست‌های فعلی و بعدی) است. قبل از هر کاری آن را بخوان و رعایت کن.

## زبان و ارتباط
- کاربر فارسی‌زبان است؛ همیشه فارسی جواب بده.
- دستورهای Git Bash را همیشه **تک‌خطی** بده (با `;` به هم بچسبان) تا با یک کپی‌پیست اجرا شوند.

## ترجیحات کاری کاربر
- نمونه اولیه اول (prototype-first): اول نسخه کوچک/تستی، بعد از تأیید، کاملش کن.
- راهنمای ستاپ شورتکات iOS باید خیلی دقیق و قدم‌به‌قدم باشد.

## واقعیت‌های محیط (مهم)
- سندباکس ممکن است بین پیام‌ها ری‌استارت شود. این‌ها بعد از ری‌استارت **می‌پرند**:
  `node_modules` ،`/tmp` ،`.env` ، و **کامیت‌های محلی گیت**.
  در عوض فایل‌های درخت کاری (worktree) می‌مانند.
- پس هرگز روی کامیت قبلی، `/tmp` یا `node_modules` حساب نکن؛ اول هر نیاز بازسازی کن
  (`npm install` + ساخت `.env` با `DATABASE_URL=postgresql://wallet:wallet@127.0.0.1:5433/wallet_db`).
- عملیات ریموت گیت‌هاب (push/PR/fetch) از این محیط انجام نمی‌شود؛ انتقال کد همیشه
  از طریق کاربر است (پچ/فایل → کاربر → گیت‌هاب).

## پروتکل انتقال کد
محدودیت‌های شناخته‌شده: (۱) کپی‌پیست از ویوور، اینتر آخر فایل را می‌اندازد؛
(۲) چت کاراکترهای U+003C/U+003E را در پیام‌های کاربر خراب می‌کند؛
(۳) attach فایل به دست ایجنت نمی‌رسد (اعلام می‌شود ولی قابل خواندن نیست)؛
(۴) درخت سندباکس ممکن است دقیقاً مساوی main کاربر نباشد (skew).

قوانین:
1. **اول recon، بعد کد.** هرگز فرض نکن main کاربر چه شکلی است. اول یک بلاک تک‌خطی
   برای وضعیت/هش فایل‌ها بده و خروجی را بگیر.
2. **پچ ایمن:** انتهای هر فایل `.patch` یک خط خالی اضافه کن و با
   `git apply --check --reverse` به‌علاوه شبیه‌سازی کپی کاربر (حذف آخرین بایت) اعتبارسنجی کن.
3. **اولویت انتقال:** (الف) پچ کم‌حساسیت ← (ب) اگر نچسبید: دستور نقطه‌ای
   (sed با شماره‌خط از روی grep خودش) ← (ج) اگر بزرگ بود: فایل کامل + بازبینی
   `git diff` قبل از کامیت.
4. **hex برای baseline دقیق:** وقتی نسخه واقعی فایل کاربر لازم است، این تک‌خطی را بده
   (خروجی ۲ خط است و در چت خراب نمی‌شود):
   `sha256sum F; (xxd -p F 2>/dev/null || od -A n -t x1 F) | tr -d ' \n'; echo`
   بعد hex را در فایل بریز، دیکد کن، با هش ادعاشده چک کن (اگر نخورد، رونویسی را چک کن)،
   سپس CRLF را نرمال و با نسخه خودت diff بگیر.
5. **هرگز از کاربر نخواه کد کامل paste کند**؛ فقط هش/شماره‌خط/hex.
6. کاربر همیشه روی برنچ جدید کار می‌کند؛ مستقیم روی main چیزی نده (به‌جز فایل صرفاً متنی).
7. بعد از مرج در main، ورسل خودکار دیپلوی می‌کند؛ چک سبز PR قبل از مرج.

## فارسی‌نویسی در کد
- تایپ حروف فارسی (ی/ک/کسره) ممکن است ناپایدار باشد: هرگز برای **مچ کردن** روی متن فارسی
  حساب نکن (از anchor انگلیسی + شماره خط استفاده کن).
- در regexها برای ی/ک از escape یونیکد استفاده کن و با تست کدپوینت اعتبارسنجی کن.

## تست
- پستگرس/داکر در سندباکس نیست؛ تست HTTP سرتاسری ممکن نیست. جایگزین: کامپایل واحد +
  تست مستقیم توابع خالص با node، بعد `tsc --noEmit` و `eslint` و `next build`.
- هر تغییری: تست رگرسیون (خروجی قبل/بعد روی نمونه‌های موجود) + کیس‌های جدید.