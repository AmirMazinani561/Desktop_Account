# AGENTS.md — قوانین کار روی این ریپو (نرم‌افزار حسابداری تحت وب)

این فایل برای ایجنت‌ها (نشست‌های فعلی و بعدی) است. **قبل از هر کاری آن را بخوان و رعایت کن.**
نسخهٔ این فایل: ۲ (۱۴۰۵/۰۶/۲۳) — بخش «واقعیت‌های محیط» و «انتقال کد» با محیط اجرایی فعلی (Arena Agent Mode) به‌روز شد.

---

## زبان و ارتباط
- کاربر فارسی‌زبان است؛ همیشه **فارسی** جواب بده.
- دستورهای ترمینال را همیشه **تک‌خطی** بده (با `;` به هم بچسبان) تا با یک کپی‌پیست اجرا شوند.
- پاسخ‌ها را با خلاصهٔ «چه چیزی تغییر کرد» شروع کن، بعد جزئیات.

## ترجیحات کاری کاربر
- **prototype-first:** اول نمونهٔ کوچک/تستی، بعد از تأیید، کاملش کن.
- **بدون کد تا تأیید:** تا کارفرما سند بالادستی (PRD + مدل داده) را تأیید نکرده، کد اجرایی ننویس.
- راهنمای ستاپ شورتکات iOS (و هر راهنمای کاربری) باید خیلی دقیق و قدم‌به‌قدم باشد.
- کد و مستندات **تمیز و مرتب** باشند؛ یک توسعه‌دهندهٔ بیرونی بدون توضیح شفاهی بفهمد.
- فایل‌های موقت (اسکریپت تست، دادهٔ آزمایشی) **بلافاصله پس از پاس شدن تست آن مرحله حذف شوند**؛ خروجی حجیم (دیتاست، بکاپ، لاگ) هرگز کامیت نشود.

## سندهای مرجع (ترتیب خواندن)
0. `README.md` — نمای کلی مخزن و وضعیت فعلی (برای ناظر بیرونی)
1. `Agents.md` (همین فایل) — قوانین کار
2. `PRD.md` — نیازمندی‌ها و نقشه راه (نسخه ۰.۴ تأییدشده)
3. `DATA_MODEL.md` — طراحی مدل داده (نسخه ۰.۳ تأییدشده)
4. `Agent_worklog.md` — حافظهٔ پروژه: تصمیم‌ها، لاگ پیشرفت، موارد باز، بک‌لاگ

هر تصمیم جدید کارفرما یا تغییر مسیر فنی → **همان لحظه** در `Agent_worklog.md` ثبت شود (فراموش‌شدنی نیست).

## واقعیت‌های محیط (به‌روز شده)
- محیط اجرایی فعلی یک **چک‌اوت واقعی گیت** است: `git` و `gh` مستقیم کار می‌کنند و احراز هویت انجام شده است.
- تغییرات فایل‌ها **پس از هر نوبت به‌صورت خودکار ذخیره می‌شوند**؛ برای ماندگاری نیازی به کامیت نیست، ولی برای تاریخچه‌گذاری کامیت بزن.
- برنچ هر نشست ثابت است (مثلاً `arena/xxxx-desktop-account`). **همهٔ کارها فقط روی همان برنچ**؛ هرگز به برنچ دیگری سوییچ نکن و مستقیم به `main` پوش نکن.
- مواردی که ممکن است بین نوبت‌ها بپرند و باید بازسازی شوند: `node_modules`، `.env`، `/tmp`، و پروسه‌های پس‌زمینه.
  → هر بار اول `npm install` و ساخت `.env` با `DATABASE_URL=postgresql://wallet:wallet@127.0.0.1:5433/wallet_db`.
- **هرگز** روی کامیت محلی قبلی، `/tmp` یا `node_modules` حساب نکن؛ اول بازسازی کن.
- اگر `git push` یا `gh` خطای احراز هویت داد: به کاربر بگو اتصال گیت‌هاب در Arena نیاز به بررسی دارد و از او بخواه آن را وصل کند. **هرگز** رمز، توکن یا کد 2FA از کاربر نخواه.
- فایل‌های متنی این ریپو با **CRLF** ذخیره شده‌اند؛ هنگام ویرایش همان را حفظ کن (اسکریپت‌های ویرایش دسته‌ای: بخوان با `newline=''`، نرمال کن، و در پایان به CRLF برگردان).

## گیت و PR
1. قبل از شروع: `git status --short ; git log --oneline -5 ; git branch --show-current` (recon).
2. کامیت با **پیام فارسی** و معنادار؛ هر مایلستون یک کامیت جدا.
3. پوش فقط به برنچ نشست: `git push origin <branch>`؛ سپس PR از همان برنچ به `main` با `gh pr create`.
4. قبل از درخواست مرج: چک‌های CI سبز + `tsc --noEmit` + `eslint` + `next build` پاس شده باشند.
5. بعد از مرج در `main`، Vercel خودکار دیپلوی می‌کند.
6. ریپازیتوری مبنا (`main` کاربر) ممکن است بازنویسی/force-push شده باشد؛ **اول recon کن، بعد فرض کن**.

## پروتکل انتقال دستی کد (فقط یدکی — وقتی push مستقیم ممکن نباشد)
اگر در محیطی بودی که عملیات ریموت گیت‌هاب انجام نمی‌شد، انتقال از طریق کاربر انجام می‌شود. محدودیت‌های شناخته‌شدهٔ آن مسیر:
(۱) کپی‌پیست از ویوور، اینتر آخر فایل را می‌اندازد؛ (۲) چت کاراکترهای U+003C/U+003E را در پیام‌های کاربر خراب می‌کند؛
(۳) attach فایل ممکن است به دست ایجنت نرسد؛ (۴) درخت سندباکس ممکن است دقیقاً مساوی `main` کاربر نباشد (skew).

قوانین آن مسیر:
1. **اول recon، بعد کد:** یک بلاک تک‌خطی برای وضعیت/هش فایل‌ها بده و خروجی را بگیر.
2. **پچ ایمن:** انتهای هر فایل `.patch` یک خط خالی اضافه کن و با `git apply --check --reverse` به‌علاوهٔ شبیه‌سازی کپی کاربر (حذف آخرین بایت) اعتبارسنجی کن.
3. **اولویت انتقال:** (الف) پچ کم‌حساسیت ← (ب) دستور نقطه‌ای (`sed` با شماره‌خط از روی `grep` خودش) ← (ج) فایل کامل + بازبینی `git diff` قبل از کامیت.
4. **hex برای baseline دقیق:**
   `sha256sum F; (xxd -p F 2>/dev/null || od -A n -t x1 F) | tr -d ' \n'; echo`
   سپس hex را دیکد کن، با هش ادعاشده چک کن، CRLF را نرمال کن و با نسخهٔ خودت diff بگیر.
5. **هرگز از کاربر نخواه کد کامل paste کند**؛ فقط هش/شماره‌خط/hex.

## فارسی‌نویسی در کد
- نام جدول، ستون، تابع و متغیر همیشه **انگلیسی** (snake_case برای دیتابیس)؛ برچسب فارسی فقط در UI و فایل ترجمه.
- تایپ حروف فارسی (ی/ک/کسره) ممکن است ناپایدار باشد: هرگز برای **مچ کردن** روی متن فارسی حساب نکن (از anchor انگلیسی + شمارهٔ خط استفاده کن).
- در regexها برای ی/ک از escape یونیکد استفاده کن (`\u06CC`, `\u06A9`, `\u0643`, `\u064A`) و با تست کدپوینت اعتبارسنجی کن.
- پیام‌های خطای روبه‌کاربر فارسی و قابل‌فهم باشند؛ کد خطا انگلیسی و پایدار.

## تست
- **پستگرس/داکر در سندباکس نیست؛** تست HTTP سرتاسری ممکن نیست. جایگزین: کامپایل واحد + تست مستقیم توابع خالص با node، بعد `tsc --noEmit` و `eslint` و `next build`.
- منطق خالص (تبدیل تاریخ شمسی، محاسبهٔ مانده، پارسر پیامک، نگاشت سرفصل) باید بدون دیتابیس تست‌شدنی باشد → آن را در توابع خالص جدا کن.
- تریگرها و قیدهای سطح دیتابیس فقط در CI (با Postgres موقت/testcontainers) تست می‌شوند.
- هر تغییری: **تست رگرسیون** (خروجی قبل/بعد روی نمونه‌های موجود) + کیس‌های جدید.
- تست‌های طلایی موتور مالی (سناریوهای `DATA_MODEL.md` بخش ۱۰) منبع حقیقت رفتارند؛ تغییرشان نیاز به تأیید کارفرما دارد.
